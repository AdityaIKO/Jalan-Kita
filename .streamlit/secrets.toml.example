# Streamlit Community Cloud reads secrets from this file (rename to secrets.toml
# in the deployment dashboard, never commit the real one). Locally the app uses
# .env instead; both are supported.

GEMINI_API_KEY = ""
ADMIN_PASSWORD = ""
